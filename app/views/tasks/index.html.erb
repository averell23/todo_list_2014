<p><%= link_to "new task", new_task_path %></p>
<p><%= link_to "Show pending", tasks_path(show: 'pending') %></p>
<p><%= link_to "Show All", tasks_path(show: 'all') %></p>

<h1>There are <%= @tasks.size %> Tasks</h1>
<div class="list-group">
  <%= render partial: 'task', collection:  @tasks %>
</ul>
